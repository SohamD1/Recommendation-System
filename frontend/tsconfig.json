{
  "compilerOptions": {
    "baseUrl": "./", // Sets the base URL for imports
    "paths": {
      "@/*": ["src/*"] // Maps '@' to the 'src' directory
    }
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
